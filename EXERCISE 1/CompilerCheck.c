#include <stdio.h>
// include is a way to import the contents of one file into the program's source file.
// the .h extension stands for header files which contains list of function to use here.

/* This is a comment. */

int main (int args,char *argv[])
{

    int distance = 100;

    //this is also a comment
    printf("you are %d miles away. \n, ", distance );

    return 0;
}